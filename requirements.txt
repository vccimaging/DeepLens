# DeepLens Requirements
# Python 3.12: python -m pip install -r requirements.txt

--extra-index-url https://download.pytorch.org/whl/cu128

# PyTorch: CUDA 12.8 on Linux/Windows, native wheels on macOS.
torch==2.10.0+cu128; sys_platform == "linux" or sys_platform == "win32"
torchvision==0.25.0+cu128; sys_platform == "linux" or sys_platform == "win32"
torch==2.10.0; sys_platform == "darwin"
torchvision==0.25.0; sys_platform == "darwin"

# Computer Vision & Image Processing
opencv-python>=4.8.0
scikit-image>=0.20.0

# Visualization
matplotlib>=3.7.0

# Deep Learning Utilities
lpips>=0.1.4
einops>=0.7.0

# Utilities
tqdm>=4.65.0
pyyaml>=6.0  # scripts/1_design_hybridlens.py, scripts/2_autolens_rms.py

# Jupyter Notebook
jupyter>=1.0.0
notebook>=7.0.0
ipywidgets>=8.0.0
